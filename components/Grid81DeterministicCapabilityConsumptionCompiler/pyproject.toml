[project]
name = "elpis-grid81-consumption-compiler"
version = "1.0.0"
description = "Grid81 Deterministic Capability Consumption Compiler (G5.3B)"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
