[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "elpis-grid81-promotion-authority"
version = "0.1.0"
description = "Explicit one-use authority bridge for Grid81 canonical promotion"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
